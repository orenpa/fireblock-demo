export enum AppRoutes {
  Home = "/",
  Dashboard = "/dashboard",
  Profile = "/profile",
  Login = "/login",
  Register = "/register",
  ErrorRoute = "/error",
}

export const { Home, Dashboard, Profile, Login, Register, ErrorRoute } =
  AppRoutes;
